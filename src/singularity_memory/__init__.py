"""Singularity Memory - top-level package for the standalone server CLI.

The Hermes plugin lives at the repository root (provider.py / __init__.py /
plugin.yaml) so Hermes' plugin loader can keep finding it. This package is the
pip-installable surface for the standalone server.
"""

__version__ = "0.2.0"
